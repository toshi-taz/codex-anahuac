# =============================================================
# CODEX ANÁHUAC — Configuración del entorno
# Copia este archivo como .env y llena los valores
# =============================================================

# Endpoint de tu proyecto en Azure AI Foundry
# Lo encuentras en: ai.azure.com → tu proyecto → Overview → Project endpoint
PROJECT_ENDPOINT=https://TU-PROYECTO.services.ai.azure.com/api/projects/TU-PROJECT-ID

# (Opcional — solo usado por agent_client.py, modo single-agent legacy)
# El sistema multi-agente (app.py / orchestrator.py) no requiere esta variable;
# los agentes se referencian por nombre directamente en cada llamada a Foundry.
AGENT_NAME=Tlacuilo
